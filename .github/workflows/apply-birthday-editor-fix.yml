name: Apply birthday editor fix

on:
  push:
    branches:
      - fix/edit-dossier-birthday

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Smoke
        run: echo birthday-fix-runner
