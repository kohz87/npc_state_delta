# NPC State Delta — agent instructions

## Authority and scope

Read in order: `AGENTS.md`, `docs/core-contract.md`, `docs/WORKPLAN.md`, and `DEVELOPMENT.md`. Read `docs/seed-provenance.md` when comparing or changing inherited behavior. Applicable nested instructions also apply.

The user's current instruction controls scope and authorization. The core contract is the single behavior authority; the workplan defines work stages, not separate runtime systems. Historical documents under `docs/history/` are evidence only and do not govern Delta. Alpha/Beta contracts do not govern this repository.

Inspect current branch, remote HEAD, working-tree changes, and available execution tools before changing files. Preserve unrelated work and concurrent commits. Complete only the requested stage or task; authorization for one stage does not authorize later stages. Do not start a rewrite simply because a later stage describes one.

## Main implementation rule

Delta was seeded from the pinned NPC State source baseline recorded in `docs/seed-provenance.*`. From the active-runtime boundary forward, every called implementation module is Delta code regardless of ancestry. Source ancestry belongs in Git/provenance records, not in runtime module classification or version-labelled filenames.

Adapt selected useful Beta/Alpha features into that foundation. Do not import their engines, mandatory trailer capture, Immediate/Development split, exhaustive field-accounting contracts, next-generation scan barrier, or cross-generation migration systems.

Keep one settings owner, scanner request dispatcher, state owner, persistence boundary, relationship mechanics owner, and evidence/injection path. These responsibilities do not require a new framework. No runtime import, remote fetch, build dependency, or installed-extension dependency on the three reference repositories.

Preserve verified baseline behavior unless the requested stage explicitly changes it. Direct supported facts may establish on first scan. Omission preserves accepted values. Personality, voice, mannerisms, agency, and relevant appearance must reach subsequent roleplay injection. Do not mistake a populated UI for a complete continuity loop.

Retain the accepted relationship formulas, caps, signed fractions, gates, and tie handling unless the user separately authorizes a scoring change. Beta priority tie handling/source-event duplicate changes are discussion options, not accepted requirements.

Keep Delta settings, globals, DOM identifiers, storage files/locks, injection key, and bundle identity isolated. Never read, import, modify, or rebuild real source/Beta/Alpha/Delta databases without explicit user authorization. Tests use temporary synthetic data. Namespace separation does not prove simultaneous automatic writers are safe.

## Active runtime and compactness

`runtime-modules.json` is the canonical machine-readable inventory of shipped JavaScript modules. Every top-level runtime `.js` file must appear there exactly once with a current semantic role. No active runtime path may be retained under a source-version or legacy-labelled filename. Packaging and validation must consume this inventory rather than maintain independent runtime lists.

Keep application versioning synchronized during release preparation. The current application version lives in `manifest.json`, `package.json`, `runtime-modules.json`, `core.js` (`NPC_STATE_VERSION`), and the `bootstrap.js` banner; README/current release documentation, changelog head, package naming, and current-version tests must agree with those authorities. Validation should enforce the machine/runtime and current release markers. Do not bump persisted schema/version families just because the application version changes, and do not rewrite historical review/provenance/old-version regression references. Unreleased implementation work does not itself authorize a version bump or release.

Trace callers, exports, event hooks, stored-data readers, tests, and packaging before deleting code. Remove superseded paths together; do not retain no-op facades, duplicate engines, or new legacy directories for hypothetical compatibility. Preserve active tombstones, source identity, writer locking, pending-write recovery, and rollback safety.

If historical-shape readers or migration logic remain necessary for supported Delta state, classify them as active Delta compatibility behavior within the current owner. Do not create a second compatibility runtime. Persisted field names or algorithms that cannot safely change without a state migration may remain only when caller/data evidence proves they are required; document that constraint and test it. Once such a reader is no longer required, remove it rather than preserving it cosmetically.

Stage 2 establishes Delta application version `0.1.0`, retires the superseded enhancement dossier-library layer, and normalizes active source-era implementation paths into semantic Delta owners such as `core-mechanics.js` and `branch-core.js`. Historical names and upstream versions remain only in Git/provenance/history records where preserving them is evidence, not runtime architecture.

Stage 3 establishes `scanner-routing.js` as the single NPC request dispatcher. Other runtime owners, including `full-cast.js`, must not patch or call the host `generateRaw` route directly; eligibility guards run before dispatch. Stage 4 establishes the current appearance model: flat appearance remains a compatibility display baseline, named forms/current-form plus an unnamed-current presentation are active Delta state, `appearance.js` provides the shared resolver, `continuity-core.js` routes dossier/portrait/injection/scanner use through it, and explicit confirmed death is terminal to automatic writers. The native editor and dedicated appearance-form controls use the same current-appearance model; Stage 4 normalization maps a manual-locked edit into the selected form so it does not become a second appearance authority. A deliberate manual Restore of a confirmed-dead dossier is treated as an explicit provenance-recorded correction of an erroneous death record, never as narrative auto-resurrection. Do not reintroduce narrative auto-resurrection or separate appearance authorities in later cleanup.

Stages 5-7 retain the pinned numerical scorer and owned recovery readers. Story-text OOC commands and their public dispatcher are removed; keep manual controls and their shared structured helpers, but never reconnect narration to add/remove dispatch. Resolved appearance uses the existing optional injection budget after essential identity/agency. All post-merge automatic writers, including focused relationships and targeted repair, must preserve terminal-death state before checkpointing. Numerical baseline verification is recorded in `docs/stages5-7-review.md`.

Stage 8 owns supporting tools only. `dossier-tools.js` may reorganize the current dossier UI, but must remain a controller over canonical state rather than a second store. Portrait upload/replacement/removal must reuse the retained portrait mutation/compression path; image generation must use the host image route independently of scanner routing and must remain preview-only until explicit application. `native-transfer.js` may extend the versioned Delta bundle manifest, but `bundle.js` remains the canonical codec/import merge owner. Native transfer is Delta-only: do not add Alpha/Beta/legacy converters. Source history may be exported for audit, but another target chat must keep its own lineage/checkpoints and must never receive invented source-message provenance. Diagnostics must remain bounded and read-only. Delta 1.0.26 records compact allowlisted per-chat operation receipts automatically; per-NPC Show/Hide is presentation-only and Data & Maintenance exports all retained receipts as versioned JSON. Profile receipts must keep candidate equality separate from evidence coverage, including `evidenceAlreadyRepresented`, and must populate those candidate/evidence flags before later batch/episode gates can reject the update, plus provider/effective reason provenance without storing whole prompts/story; birthday receipts retain the existing bounded chronology needed to explain a relevant hold/advance. This telemetry is never canonical story/profile state and must not affect scanning, persistence, prompts, rollback or native transfer. Report actual dispatcher counts where exposed, label character-derived token values as estimates, and never retain whole story transcripts, credentials, full private prompts or full provider responses. `docs/stage8-supporting-tools.md` records the accepted Stage 8 ownership and live-host verification boundary.

Stage 9 completes consolidation and integrated release verification without redesigning the working Gemini prompt. Preserve model-facing wording, schemas, evidence/context coverage, output allowances, reasoning settings and automatic request/retry budgets by default. Any necessary clarification needs a concrete ambiguity and recorded semantic/budget comparison. Up to five review/fix cycles are permitted; stop after a complete clean review and passing required verification. Record findings, dispositions and checks in one `docs/stage9-review.md` and accumulate fixes in one release changelog entry. Keep application versions separate from persisted formats.

The subsequently authorized calendar/birthday and form UI amendments are accepted functionality: optional era, ordered month table, optional complete fallback clock, deterministic birthday generation and owned structured-date arithmetic. Apparent age remains separate from chronology, but when owned calendar arithmetic proves an existing NPC's birthday rollover, an already-compact numeric apparent-age estimate may follow the same proven delta unless manually locked or explicitly evolved; it never supplies chronological inference. Appearance/life-state edits call canonical scoped APIs, not whole-dossier imports. The maintained Portrait surface is prompt/upload based; the retained host-image preview/application API remains independent of scanner routing. UI notifications and selected-record reads must not reintroduce document-wide polling or history cloning.

Do not silently loosen or tighten accepted evidence interpretation under cleanup. Keep model-facing instructions compact. Do not add unsolicited scans, retries, per-field calls, classifiers, or summarizers. Account for retained focused passes as real requests.

## Verification and publication

Verify retry-to-permanent-failure transitions through cache eviction, undurable hydration and later flush, including newer in-flight mutations, cancellation and revision conflicts. Exercise failed/skipped/busy scan receipt boundaries through the synthetic host, explicit swipe-sibling revisits, net-zero same-message coalescing, deep/sequential deletion with predecessor retention, and scattered delete/regenerate replacements under checkpoint-byte pressure. Delete/edit replacements must remain linear and exact-boundary only: journal or checkpoint recovery may restore `divergence - 1`, but no older ancestor/root may substitute for a missing parent. Near-tail replacement divergence must recover exactly when proof exists; deep edits with multiple retained assistant descendants must fail closed and preserve canonical dossiers instead of rewinding an unreplayable suffix. Only explicit host swipes retain sibling history; exact known siblings may restore directly, while unseen siblings may use only their exact parent/root anchor and then rescan. Versioned legacy branch compaction must preserve the live rollback-head predecessor chain plus host-proven swipe ownership, defer across unresolved destructive divergence, and keep the configured 8 MB aggregate / 2 MB single-checkpoint limits independent from the 256-message rollback-journal contract. Important-Bond verification must include repeated reconciliation of malformed extra-pipe/spaced-slash input and manual delete followed by full social reconciliation; normalization must be idempotent and the deleted shared pair must not be recreated from mirrored dossier text. Boundary verification must also prove canonical bonds can exceed the former 220-character ceiling within the 360/260 storage budgets, never end in a severed word or dangling structural separator, drop over-budget trailing fragments whole, and stay idempotent through graph projection and manual locks. Durable-profile verification must prove accepted explicit `refine` is a full current summary/list: omitted old clothing/personality/speech/mannerism/behavior rules do not survive, while missing/`keep` lifecycle markers cannot retire omitted established clauses and must still pass Personality/Speech field-specific safety. Unsafe Behavioral Profile proposals preserve the established profile when protected agency/morality reversals are mixed across renamed categories or expressed through bounded negation, while legitimate contextual cooperation remains allowed; refining one named appearance form leaves unrelated forms unchanged. Gradual durable-development verification must prove unlabeled/semantically equivalent Personality/Speech evidence can accumulate without exact label spelling, only real tagged messages from the supplied Refresh window count independently, a ledger-ready changed candidate can be promoted from `refine`/`keep` only when its new concepts are evidence-grounded, and grounding may use only the bounded samples attached to independently counted observations rather than replayed paraphrases or arbitrary transcript history. Replaying one source message cannot add a second historical sample; v1 ledgers may seed one sample from their prior `latestEvidence`; an unchanged stale candidate remains unapplied with explicit diagnostics; manual locks still block accumulation; and cross-chat import clears pending Personality/Speech chronology and samples. Delta 1.0.23 verification must also prove bounded time-skip episodes can reach later NPC montage segments without crossing a distinct later episode, each NPC owns its own evidence, one NPC cannot authorize another, differently-labelled independent observations may reach aggregate readiness only when they collectively ground the changed Personality/Speech candidate without merging concepts, and proven inferred-batch handling reaches Mannerisms/Behavioral Profile while bare passage remains inert. Delta 1.0.24 verification must additionally prove a missing provider `developmentReason` can be recovered only from a grounded field reason or candidate-specific field evidence, unrelated Mannerism/Behavior evidence cannot authorize a different newly proposed entry, and birthday diagnostics expose bounded calendar/birth-year detail for relevant rows. Delta 1.0.25 verification must prove that candidate equality alone never consumes unresolved Personality/Speech evidence, `evidence-already-reflected` requires every bounded evidence claim to be represented by current canon, novel copied-candidate evidence remains pending as `waiting-for-revised-candidate`, unchanged Mannerism/Behavior lists retain novel evidence, and genuinely redundant evidence can still resolve without cosmetic rewriting. Delta 1.0.26 verification must additionally prove that an unchanged provider-declared batch candidate is classified before explicit/batch episode authorization, so failed episode grounding cannot produce `waiting-for-explicit-gate` or null/misleading candidate-evidence diagnostics; a targeted Refresh may bridge a long montage only through validated `[mN]` source provenance from the nearest prior elapsed-time anchor through the target's cited messages, while ordinary multi-NPC scans retain the generic bounded episode cap and cross-NPC isolation. Delta 1.0.27 verification must additionally prove targeted Refresh places stable rules/schema and identical tagged history before target-specific authority, ordinary scans move only invariant compact Stage 4 content before dynamic dossier data, all supplied evidence/source-tag order/locks/current-exchange boundaries remain intact, request counts/output allowances/routing stay unchanged, and common-prefix character measurements are reported without claiming a provider cache hit. Delta 1.0.28 verification must additionally prove unsupported new durable meaning cannot ride through `refine`, current flat Appearance persists in its canonical selected slot, Shared appearance deduplication preserves distinct/negated detail, collective Important Bonds retire only after proven membership, Mannerism/Behavioral Profile reorder does not count as semantic change or consume novel evidence, evidence coverage requires the substantive body rather than a matching label, and the 1.0.27 prompt/request hashes and counts remain unchanged. Cross-chat native-transfer verification must rebase pending Personality/Speech development source-message/turn provenance while preserving accepted current characterization, preserve target activity counters for matching dossiers, seed newly admitted foreign activity at the target import turn, and never compare foreign source counters directly against the target clock. Social verification must prove inferred edges promoted by explicit/manual evidence clear stale inferred status through duplicate/reversed normalization and cannot be displaced by equal-or-weaker arrivals at full graph capacity. These checks enforce C03/C04/C05/C08/C09 and the social/profile-continuity rules of `docs/core-contract.md`; they do not create separate behavior rules.

Follow `DEVELOPMENT.md` for executable commands. Test changed behavior through production functions and the existing synthetic host harness. When storage, identity, scan scheduling, or scoring changes, exercise relevant stale-result, duplicate, swipe/edit/delete, failure, and recovery cases.

Distinguish deterministic tests, synthetic host checks, actual browser/provider checks, estimated tokens, and provider-reported usage. Mocked success is not proof of live extraction quality, latency, or UI responsiveness. Do not use a test count alone as acceptance evidence.

When execution is unavailable, continue authorized edits/reviewable work and report unrun checks; inspect ordinary CI for the exact candidate where possible. Do not create temporary workflows or empty commits solely to obtain execution. Never claim an earlier commit validates current changes.

This file does not independently authorize publishing, releases, force pushes, merges, or database operations. Respect existing session authorization. Re-read remote state before authorized publication, never force over concurrent work, and verify the final remote commit. Retain the existing GPL license and relevant source notices.

Report what changed, current stage status, actual verification, material limitations, and commit/PR link when created. Do not describe planned stages or a development seed as a completed release.
